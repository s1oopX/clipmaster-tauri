{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "main",
  "description": "Main app window permissions",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-close"
  ]
}
