# Changelog

## 0.1.8

### Features

- 搜索升级为 SQLite FTS5 trigram 全文索引（迁移 v7）：中文/英文子串检索走索引，≥3 字符查询用 `MATCH`，更短查询自动回退 `LIKE`，语义与旧版一致；触发器保持索引与主表同步，旧库自动回填。
- 搜索默认覆盖全部历史：未选日期时不再只搜当天分区，选定日期时仍按日过滤。
- 遵守剪贴板隐私协议：内容带 `ExcludeClipboardContentFromMonitorProcessing` 或值为 0 的 `CanIncludeInClipboardHistory` 标记时（密码管理器复制密码的标准行为）不记录、不读取。
- 新增单实例守卫：重复启动时唤起已运行实例的主窗口，避免双份记录与快捷键注册冲突。
- 启动后台清扫孤儿文件：回收删除失败遗留的无主图片和上次运行残留的冻结截图缓存。

### Fixes

- 关于面板显示版本同步到 `0.1.8`。
- 图片去重哈希改为全量计算：修复采样哈希导致同尺寸小差异图片（计时器数字、光标等）碰撞后新图被静默丢弃的问题；哈希仅在剪贴板序列号变化时计算一次。
- `settings.json` 改为临时文件 + fsync + 原子重命名落盘，写入中断不再损坏设置；设置文件损坏时的回退不再静默关闭剪贴板监听。
- 剪贴板监听任务增加 panic 监督自动重启，数据库锁毒化自动恢复，单次异常不再永久停止记录。
- 修复自写回抑制的陈旧哈希竞态：程序写入被更新内容覆盖后，滞留的抑制标记不再误吞用户 5 秒内的真实复制。
- 通过 `CLIPMASTER_APP_DATA_DIR` 覆盖数据目录时，启动时把实际数据目录的 `images/` 与 `screenshot-cache/` 动态加入 asset 协议 scope，自定义目录下图片预览与冻结截图不再 403。
- 贴图窗口创建时不再抢占焦点，游戏或输入法中的 Esc 键不会误关贴图。
- 全局快捷键改为独立注册：一个快捷键被其他程序占用不再连累另一个全部失效。
- 图片记录插入数据库失败时回收已落盘的原图与缩略图；无托盘环境下关窗退出也会正确收尾会话。
- 标注与收藏解耦：加标注不再强制收藏（删标注后遗留的"幽灵收藏"不再产生），带标注的记录改由清理逻辑直接保护。
- 前端缩略图加载改为写时合并 + 并行解析 + 失败负缓存，重叠加载不再互相覆盖，缺失图片不再反复请求；原图缺失时不再打开空白查看器。

### CI

- Release Build 在 tag 推送时自动把 CI 构建产物挂载到 GitHub Release（不存在则创建草稿），发布资产可追溯到 CI 构建。
- 修复依赖审计告警（postcss 升级）。

### Docs

- 中英 README 新增界面预览截图（主窗口 / 设置面板 / 桌面贴图），素材为演示数据。
- 新增 `docs/SIGNING.md`：代码签名供应商对比（SignPath Foundation 首选）与接入步骤；Release Build 工作流内置可选 SignPath 签名 job，凭据未配置时自动跳过。
- 刷新 `docs/ARCHITECTURE.md` 模块化现状与 `docs/DATABASE.md` 全文搜索章节。

## 0.1.7

### Features

- 截图标注新增文字、步骤编号和橡皮擦工具；橡皮擦删除标注后可继续使用撤销/重做恢复操作。

### Fixes

- 修复 asset 协议 scope 误将应用标识符拼接两次（`$APPDATA/com.clipmaster.desktop/...`），导致图片缩略图、原图预览与冻结截图画面在移除 base64 兜底后全部 403 无法显示的问题。Tauri v2 的 `$APPDATA` 已解析为「数据目录/应用标识符」，scope 修正为 `$APPDATA/images/**` 与 `$APPDATA/screenshot-cache/**`，并同步更新安全配置测试。
- 关于面板显示版本同步到 `0.1.7`。

### Docs

- 中英 README 全面重写：新增系统架构图、五层安全模型、数据与存储、截图管线和工程质量门禁章节。
- CI 的 clippy 门禁补齐 `--all-targets`，与文档表述一致。

## 0.1.6

### Features

- 新增主窗口全局快捷键，默认 `CommandOrControl+Shift+Space`，可呼出/隐藏主窗口并在呼出后聚焦搜索框。
- 设置面板支持同时查看和录制截图快捷键、主窗口快捷键，并禁止两者使用相同组合键。
- 截图标注新增模糊、马赛克遮挡和撤销/重做，方便在保存到历史和复制到剪贴板前处理敏感区域。
- 新增成熟截图工具对照文档，明确 ClipMaster 截图功能已经补齐的能力和后续不扩张的边界。

### Fixes

- 启动区域截图前会先隐藏可见的主窗口并等待窗口退出画面，避免冻结截图包含 ClipMaster 自身；截图结束后只在本次启动截图前主窗口可见时恢复主窗口。
- 关于面板显示版本同步到当前 `0.1.6` 基线。
- 整理数据库迁移辅助函数位置，恢复 `cargo clippy --all-targets -- -D warnings` 质量门。

### Tests

- 补充主窗口快捷键聚焦搜索、双快捷键录制、快捷键校验、重复组合键拒绝、截图恢复和截图标注工具测试。

## 0.1.5

### Fixes

- 图片历史预览改为优先使用后端生成的 data URL，避免 asset protocol 路径可用但前端仍加载失败。
- 保留原有的文件路径解析兜底，减少旧记录或特殊文件场景下的预览失效。

## 0.1.4

### Fixes

- 修复图片预览 URL 由前端拼接绝对路径导致的加载不稳定问题，改为后端校验并解析真实图片资产路径。
- 当图片记录路径已经过期但同名文件仍存在于旧图片目录时，自动回退到可用文件，减少“图片预览不可用”的误报。
- 图片资产解析继续限制在应用数据目录的 `images/` 下，避免放宽本地文件访问范围。

### Tests

- 补充图片资产解析、旧路径回退和缺失文件返回空结果的 Rust 单元测试。

## 0.1.3

### Fixes

- 修复旧图片记录或缺失缩略图时预览失效的问题，支持自动回退到原图预览。
- 修复缩略图和原图都不可用时界面长期停留在“图片加载中”的问题，改为显示明确的不可用状态。
- 修复从历史记录双击复制或点击复制时被剪贴板监听器重新保存、刷新存储时间和改变排序的问题。
- 统一截图保存、图片复制和剪贴板监听的图片哈希逻辑，减少同图从不同路径进入时产生重复记录的情况。

### Tests

- 补充图片预览降级、图片预览失败状态、剪贴板自写入抑制、pending 过期和图片哈希稳定性测试。

## 0.1.2

### Features

- 重构截图流程：启动截图时先冻结当前屏幕，再在静态画面上选区，提升截图稳定性。
- 截图确认后默认自动复制到系统剪贴板，同时保存到历史记录，支持直接 `Ctrl+V` 粘贴。
- 截图区支持拖动移动、8 个控制点缩放、方向键 1px 微调、尺寸显示和重新框选。
- 截图标注新增矩形、箭头和画笔，最终输出会包含标注内容。
- 主应用窗口固定为 `440 x 560`，保持桌面工具布局稳定。
- 新增开机自启动设置（可选），用户可以在常规设置中启用或禁用。
- 启用开机自启动后，应用会在 Windows 启动时自动运行。

### Docs

- 将中英文 README 更新为更正式的开源项目介绍，补充下载、功能、隐私、开发和项目结构说明。
- 更新 GitHub 仓库描述、主页和 topics，明确本地优先、Windows、截图和剪贴板管理定位。

### User Trust

- 高级设置新增“清空全部历史”危险操作，支持一次性删除全部剪贴板记录、收藏、置顶、标注、图片原图和缩略图。
- 清空全部历史需要二次确认，执行后会刷新当前列表、日期索引和活动会话计数。

### Tests

- 补充前端确认流程测试和 Rust 数据库全量清空测试。

## 0.1.1

### Release

- 公开 GitHub 仓库，补充正式项目图标并重新生成 Tauri 图标资产。
- 将应用版本提升到 `0.1.1`，用于发布包含新图标和用户文档的补丁版安装包。
- 为 Release 安装包补充 SHA256 校验文件。

### User Trust

- 主界面新增“暂停/恢复”剪贴板监听入口，便于复制敏感内容前临时停用记录。
- 关于面板显示实际应用数据目录，帮助用户定位历史数据库、设置和图片文件。
- 新增 `docs/PRIVACY.md` 和 `docs/FAQ.md`，说明本地数据、暂停记录、卸载清理和校验安装包。
- 更新 README 下载说明，明确当前未代码签名以及如何使用校验文件。

## 0.1.0

### Build

- 将 Tauri identifier 改为 `com.clipmaster.desktop`，移除 `.app` 结尾警告。
- 启动时迁移旧数据目录 `com.clipmaster.app` 到 `com.clipmaster.desktop`，避免升级后历史记录丢失。
- 增加 `schema_migrations` 表，记录旧 schema 升级、日期回填和图片路径迁移。
- 增加 `CLIPMASTER_APP_DATA_DIR`，用于隔离打包版冒烟测试的数据目录。
- 增加系统托盘；关闭主窗口时隐藏到托盘，托盘菜单支持显示和退出。
- 修复前端重复导出导致的 Vite 构建失败。
- 补充 `esbuild` devDependency。
- 将 Vite build target 调整为 `esnext`，匹配现代 Tauri WebView。
- 修复 Rust `search_items` 查询生命周期错误。
- 清理关闭窗口时的当前会话内存状态。
- 验证 `npm run tauri:build` 可以生成 exe、MSI 和 NSIS 安装包。

### Docs

- 删除一次性开发报告、过期搭建说明和重复构建说明。
- 保留面向后续维护的文档集合。
- 新增 `docs/NEXT_STEPS.md`。
- 更新 README、Roadmap、API、Database、Architecture、Workflow 和 Troubleshooting。
- 同步图片复制、自定义清理、数据目录迁移和图片文件生命周期的当前实现状态。
- 补充中英双语 README、MIT License，并将 `src-tauri/Cargo.lock` 纳入版本控制以便复现构建。

### Tests

- 新增 Vitest + Svelte Testing Library 测试环境。
- 为主界面增加搜索、筛选、空状态、记录列表和操作按钮的 UI 行为测试。
- 为旧数据目录迁移和图片文件清理补充 Rust 单元测试。
- 为托盘主点击判断补充 Rust 单元测试。
- 完成打包版受控冒烟，验证临时数据目录、旧目录迁移、迁移版本表和文本剪贴板监听。
- 完成打包版托盘关闭冒烟，验证主窗口 `WM_CLOSE` 后进程留在托盘且窗口隐藏。
- 补齐完整 UI 自动化冒烟，覆盖图片预览/复制、搜索、收藏、置顶、删除和重启加载。
- 完成打包版重启持久化烟测，验证隔离 DB 的历史记录在重启后仍存在。
- 完成打包版真实 WebView 图片预览烟测，验证 `convertFileSrc` 生成的本地缩略图可渲染。

### UI

- 将主界面调整为桌面工具布局：左侧筛选栏、顶部搜索栏、紧凑历史列表。
- 使用 `@lucide/svelte` 替换大部分 emoji 图标。
- 为搜索框、筛选导航、图片预览和记录操作按钮补齐可访问标签。
- 压缩窄窗口顶部区域，优化图片记录空状态和列表项操作区密度。
- 整理前端结构：将 `App.svelte` 样式迁到 `src/app.css`，抽离配置常量、UI 工具函数和低耦合组件。

## 0.1.0-alpha

### Added

- Tauri 2 + Svelte 5 + Vite 8 项目结构。
- Rust 剪贴板轮询服务。
- SQLite 数据库和会话管理。
- 剪贴板文本和图片记录。
- 搜索、删除、收藏、置顶。
- 图片文件保存和前端预览。
- 文本复制回剪贴板。
